# Report

This directory contains the Typst code to compile the final report and presentation.

To compile, change into the respective repository and run

```bash
typst compile <file>.typ
```

You can also auto-compile when saving! To do this, run

```bash
typst watch <file>.typ
```
